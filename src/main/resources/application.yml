spring:
  datasource:
    url: jdbc:mysql://localhost:3306/badcomments?serverTimezone=UTC&characterEncoding=UTF-8
    username: badcomments_user
    password: 비밀번호
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: "change-this-to-a-very-long-random-secret-key-at-least-32-bytes"
  expiration-millis: 3600000  # 1시간 (1000 * 60 * 60)
